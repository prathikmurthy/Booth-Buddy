//sample text field
import 'package:flutter/material.dart';

//create a class called Page1 that extends the StatelessWidget and contains the text "Hello World"
class Page1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("Screen 1", style: TextStyle(fontSize: 36.0));
  }
}
